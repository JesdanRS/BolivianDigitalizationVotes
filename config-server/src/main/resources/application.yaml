server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/LuwuVelasco/BolivianDigitalizationVotes-Config
          default-label: main